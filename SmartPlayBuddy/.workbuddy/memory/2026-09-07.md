# 2026-09-07

## daily_task Mod 逻辑改为转发驱动 daily_task/start

- 需求：将 mods/starrail/mod.py 中 `operate == "daily_task"` 分支从「Mod 手动编排（observe → 逐项 map → reward）」改为直接转发驱动 `daily_task/start`，由驱动侧 `start_daily_task()` 完成完整流程（观察 → 逐项执行 → 领取奖励）。
- 驱动层 `src/smartplaybuddy/drivers/starrail/driver.py` 已有 `daily_task/start` operate（调用 `start_daily_task()`），因此 Mod 层只需转发。
- 改动文件：
  - `mods/starrail/mod.py`：`daily_task` 分支改为 `dispatch("daily_task/start")`，返回 `{started}`；更新模块 docstring 与进度事件（"开始执行每日实训"/"每日实训完成"）。
  - `tests/test_starrail_mod.py`：更新 daily_task 相关测试（真实负载透传、full_sequence、no_tasks、observe_only、新增 passthrough、events、mod 集成测试），均改为断言只转发一次 `daily_task/start`。
  - `mods/starrail/README.md`：更新 operate 表 `daily_task` 说明。
- 测试：`pytest tests/test_starrail_mod.py -k "daily_task or ping"` → 12 passed。
- 注意：`test_starrail_mod.py` 中 5 个 `clear_power` 相关测试仍失败（历史遗留——测试断言与实际 mod.py 的 clear_power 实现不一致：测试期望"查询开拓力+体力不足跳过"，实际 mod.py 只是直接 dispatch power/clear）。与本次改动无关，未处理。
- 环境：pytest 需用系统 Python `C:/Users/hayc8/AppData/Local/Programs/Python/python313/python.exe`（managed python 无 pytest）。
## 2026-09-07
- 在 mods/starrail/mod.py 的 `_on_starrail_command` 中加入 print，控制台直接显示 mod 收到的 operate/params。
- 确认：mod 是独立进程，print 直接进控制台；stdout shim（STARRAIL_DEBUG）只影响驱动子进程（--driver-host），其 print 被重定向到 driver_debug.log。
## 2026-09-07（续）：修复 mod 收不到前端参数
- 根因：web/backend/bridge.py `_run_command` 发送扁平 payload（operate 与参数混在一层），而 mod 按 `Data={"operate":..,"params":{...}}` 嵌套契约解析（controller.py 与 tests 均为嵌套格式），导致 `data.get("params")` 恒为空。日志证据：`params_data:{}`。
- 修复：bridge 改为发送嵌套格式；mod `_on_starrail_command` 增加扁平格式兼容（params 缺失时把除 operate 外顶层字段视为参数），并加日志 `收到指令数据 operate=... params=...`。
- 已用最小脚本验证嵌套/扁平两种格式解析均通过。
- 注意：tests/test_starrail_mod.py 有 5 个 clear_power 相关测试失败，系用户此前重构 execute_starrail_flow 时移除了 power/query 预检查（测试期望 calls[1]=power/clear），与本次修复无关，待用户决定是否恢复预检查或更新测试。

## 2026-09-07 前端重构（Vue → 纯 HTML/JS）
- 删除 `web/frontend` 下的 Vue 3 + Vite 工程（src/、dist/、node_modules/、package.json、vite.config.js）。
- 重写为纯 HTML + JS（免构建）：`index.html`、`css/style.css`、`js/{api,app,daily,power}.js`，保留暗金主题与原功能（daily_task 观察/勾选执行/领奖 + clear_power 滑杆0-30/战利品类型/后备开拓力 + Job 进度轮询）。前端使用 ES Modules。
- `web/backend/app.py`：静态托管改为直接挂载 `web/frontend`（`StaticFiles(html=True)` 挂在 `/`，API 路由先注册优先匹配），移除 dist/SPA 回退与 FileResponse 依赖。
- 验证：后端 8765 启动后 `/`、css、4 个 js 均 200，`/api/auth/me` 正常返回统一登录 URL。
- README.md 已同步更新（无需 npm 构建）。

## 2026-09-07 Web 前后端测试
- 新增 `tests/test_web_backend.py`（18 个用例全部通过）：静态托管、认证回跳/退出、参数校验（422/400/401/404）、daily_task 与 clear_power 的 params 组装（mock bridge）、Job 轮询。运行方式 `python -m pytest tests/test_web_backend.py -v`，无需游戏链路与统一登录网络。
- 测试环境：托管 venv（C:/Users/hayc8/.workbuddy/binaries/python/envs/default）补装了 httpx、pytest。
- 更新 `docs/zh-CN/web-api-testing.md`：去掉 npm/dist 旧说明，补充自动化测试章节。
