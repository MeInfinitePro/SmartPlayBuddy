# 2026-09-09

## Web 前端换肤为“三月七小助手”风格

- 参考对象：March7thAssistant（三月七小助手，github.com/moesnow/March7thAssistant）。
  已下载其官方界面截图确认风格：浅色底 + 白色圆角卡片 + 三月七粉主色 + 深蓝文字 + 星星点缀 + Fluent 感。
- 修改文件：
  - `web/frontend/css/style.css`：整体重写。浅色二次元主题（--pink #ff7fae/#f75d97 主色、白色卡片、柔和阴影、圆角 12-16px、渐变 hero 横幅、✦ 星星装饰、粉色按钮/滑杆/单选复选、任务 chip 选中态粉边粉底）。原有类名全部保留。
  - `web/frontend/index.html`：仅改 `<title>` 与 h1 为“三月七小助手 · SmartPlayBuddy”，meta 中加“星穹铁道助手”文案。布局、DOM 结构、元素 ID 全部未动。
  - JS（app.js/api.js/daily.js/power.js）零改动，功能与接口完全不变。
- 验证方式：python -m http.server 托管 web/frontend，agent-browser 打开页面并注入演示数据（强制显示主面板）截图验证两个面板效果。
- 预览截图存于 `docs/ui-preview/theme-daily.png`、`docs/ui-preview/theme-power.png`。
- 注意：静态预览时 Tab 切换不生效是正常的（initTabs 在后端登录成功后才初始化）；实际经 `python web/backend/app.py`（默认 8765 端口）访问时一切正常。

## 前端重构：白底 + 左侧导航（撤销上一版换肤）

- 用户要求撤销上一版浅粉皮肤，重构为三月七小助手布局：**整体白色背景 + 左侧导航栏（主页/帮助/启动游戏/日志/设置）**。
- 主页仅保留两个功能模块卡片与执行按钮：「执行完整日常」「开始清体力」；清体力计划（滑杆 0-30、战利品类型、后备开拓力、查询开拓力）移入「设置 → 体力计划」，主页卡片实时同步计划文案。
- 「日志」页为会话级运行日志（js/log.js 的 pushLog/clearLog，任务事件增量同步，最多 300 行）；「启动游戏」为占位入口（远程启动需客户端支持，点击仅记日志）；「帮助」页为使用说明。
- 文件：index.html 全部重写（#shell = aside#sidebar + main#content，hash 路由 #home/#help/#launch/#log/#settings）；style.css 全部重写（--bg #fff、--pink-deep #f0528c、nav-item active 粉底 + 左侧粉条）；app.js 重写（登录态 + 导航 + 退出/清日志/启动游戏）；daily.js 重写（仅 runDaily + startJob）；power.js 重写（initPower：设置页控件 + 主页执行 + 查询开拓力）；新增 js/log.js；api.js 未动。
- 删除了旧 UI 的「观察任务/勾选任务 chips/仅领取奖励/独立 Tab」交互（后端 API 仍在，前端不再调用 observe/reward）。
- 预览验证：临时 mock 服务器（伪造 /api/auth/me）+ agent-browser 截图，5 个页面全部通过；计划同步（滑杆 input → 主页 plan-hint）验证 OK。
- 预览截图更新至 `docs/ui-preview/`（home/settings/log/help/launch.png，删除旧 theme-*.png）。
- 复用技巧：mock 静态预览服务器脚本在 `C:\Users\hayc8\AppData\Local\Temp\mock_preview_server.py`（伪造登录态 + 静态托管 frontend），配合 agent-browser 可无后端完整预览 SPA。

## 微调：正文文字改为黑色

- 用户要求文字黑色：CSS 变量 --text/#000、--indigo/#000、--muted 近黑 #1a1a1a；hero h1 由粉紫渐变改纯黑；eyebrow、nav-item 默认、help 加粗、slider 数值等均改黑。粉色/绿色仅保留于按钮背景、激活项、状态标签（tag）与错误提示等强调元素。

## 改动：按钮换大众蓝 + 未登录直接跳统一登录

- 用户反馈“运行起来看不到前端页面效果”：根因是未登录时前端停留在“尚未通过统一登录验证”提示页，功能壳（shell）在登录成功后才显示。
- 已改为：未登录（/api/auth/me 返回 authenticated:false 且有 loginUrl）→ 前端 location.replace(loginUrl) 直接跳统一登录，无中间提示页；仅两种情况显示兜底错误卡：① 登录回跳失败（?authError=...，防死循环）；② 后端拿不到 authorize 地址。app.js boot 分支重写，新增 showAuthFail()；index.html 登录门改为精简错误卡。
- 按钮颜色从三月七粉换为大众蓝：主按钮 linear-gradient(#2f7bf6→#2563eb)，激活导航/滑杆/复选/输入聚焦 #2563eb 系（--brand/--brand-deep/--brand-soft）；tag.pink 类更名为 tag.accent；文字保持黑色。
- 已用 mock SSO 流程（/sso 页 + /api/auth/callback 设 cookie 回跳）全链验证：/ → 自动跳 /sso → 模拟登录 → 回 / → shell 出现；authError 兜底页验证通过。
- 预览截图更新 docs/ui-preview/（home/settings/launch + auth-fail/sso-redirect）。

## 修复：Cannot read properties of null (reading 'style') @ boot

- 用户报错 app.js:53:18 读取 null.style——行号对不上磁盘文件，根因是浏览器缓存了旧版 app.js/HTML，DOM 结构不匹配导致 boot 中断、页面停在加载卡。
- 三重修复：
  1. `web/frontend/js/app.js`：boot 登录成功后先校验应用壳关键 DOM（shell/nav/user-id/btn-logout/btn-run-daily/btn-clear-power/power-num/log-console/btn-clear-log/btn-launch-game），缺失则 showFatal() 提示“页面版本不一致 + Ctrl+F5 强刷”，不再抛 null 异常；showFatal/showAuthFail 全部空值防御，极端情况向 body 注入全屏提示条；boot().catch 兜底。
  2. `web/frontend/index.html`：head 加 no-cache meta（Cache-Control/Pragma/Expires）；css/js 资源加 ?v=20260909 版本号。
  3. `web/backend/app.py`：新增 @app.middleware("http") _no_cache_frontend，对 /、*.html、*.js、*.css 返回 Cache-Control: no-cache, no-store, must-revalidate。
- 验证：py_compile 通过；mock 服务器 + agent-browser 单链跑通登录回跳（shell=flex/nav=5/欢迎日志），新版 app.js 确认被服务端返回。

## 前端迁移：纯静态 → Vite + Vue3 工程化

- 用户确认方案：Vite + Vue3 工程化；旧免构建前端归档为 `web/frontend-legacy/`（保留存档，不再被托管）。
- 新工程 `web/frontend/`（vue ^3.5 / vite ^6 / @vitejs/plugin-vue ^5.2，34 包）：
  - `vite.config.js`：dev 5173 + /api 代理到 8765；build → dist/。
  - 布局/视觉/功能完全不变：style.css 原样迁移；App.vue 登录状态机（me→跳统一登录/兜底卡）+ 左侧导航壳 + hash 路由；页面全部 v-show 保活（轮询状态不丢）；DailyCard/PowerCard 复用 composables/useJobRunner.js（pollJob 轮询 + 进度 + 日志增量同步，行为对齐原 daily.js/power.js）；全局 store：log.js / settings.js（targetClient + activity 忙碌开关）/ powerPlan.js（设置页编辑、主页实时同步）。
  - ProgressBox.vue 统一进度框渲染（v-for 行替代 innerHTML，自动转义）。
- 后端 `app.py`：静态托管改为 `web/frontend/dist`（缺失时启动打印 build 提示）；py_compile 通过。
- 构建：npm install（34 包）+ npm run build 成功（gzip JS 33.6KB / CSS 2.4KB）。
- 验证：mock dist 服务器（8903）+ agent-browser 单链走通登录→主页→设置/日志/启动/帮助五页截图，视觉与 legacy 一致；滑杆同步修复——Vue3 对 range 的 v-model 监听 change（松手才触发），改为 :value + @input 手动绑定恢复实时同步（与原版一致）。
- 经验：该 agent-browser CLI 跨 bash 调用不保留登录态/tab，验证必须单条命令链完成（open→eval→screenshot 串联）。
- 预览截图已更新 docs/ui-preview/（home/settings/log/launch/help，Vue 版）。

## 调试：登录验证加 SKIP_AUTH 开关

- 用户要求注释登录验证：`App.vue` boot() 顶部加 `const SKIP_AUTH = true`——开启时跳过 /api/auth/me，直接进 shell（userId=「本地调试」）；原统一登录逻辑原样保留，改回 false + npm run build 即恢复。
- 注意：用户自行把 src 下公共模块移到了 `src/js/`（App.vue import 已改为 ./js/*），侧栏品牌文案被改为「星穹铁道助手 / StarRailAssistant」。
- 验证：纯静态托管 dist（无后端）打开即入壳，欢迎日志正常。

## 主页改版：三月七小助手图标点击式主页

- 用户要求：参照三月七小助手主页，big.png 作主页背景，1.png 作每日实训背景图，2.png 作清体力背景图（实际 public 只有 2.webp，用 /2.webp），点击图标即执行任务，日志在「日志」菜单查看。
- 改动：
  - `HomeView.vue`：整页 `.home-page` 背景 url('/big.png') cover，居中 hero 标题「三月七小助手」，下方 module-grid 两卡片。
  - `DailyCard.vue` / `PowerCard.vue`：改为角色图标卡片（`.tile-btn` 整卡可点、img.tile-bg 为 1.png / 2.webp、底部渐变遮罩 + 白字标题、右上 ▶ 圆形徽标、busy 时 ⏳ + 脉冲动画 + 禁用），移除原文字执行按钮；执行逻辑仍走 useJobRunner，进度走 ProgressBox，事件照旧增量同步到日志页。
  - `style.css`：新增 .home-page/.home-hero/.tile-* 样式段（旧类保留，其他页面不受影响）。
- 构建：npm run build 成功；后端 StaticFiles 实时读 dist，无需重启即生效（8765 端口已有旧实例在跑）。
- 后端依赖：系统 python 无 uvicorn；用受管 venv `C:\Users\hayc8\.workbuddy\binaries\python\envs\default`（已含 uvicorn/fastapi）可启动 `web/backend/app.py`。
- 经验：public/ 图片构建时自动拷入 dist；角色小图（200px）做整卡背景放大后略糊，可接受。

## 主页布局二次调整：背景横幅 + 正常大小图标

- 用户反馈：背景图和功能图标分两部分，背景图在上面占绝大部分，功能图标调整为正常大小。
- 改动：
  - `HomeView.vue`：顶部 `.home-banner`（min-height clamp(360px, 58vh, 560px)，big.png cover，底部白色渐变 ::after 保证标题可读，hero 左下角对齐），下方 module-grid 功能卡片。
  - `DailyCard.vue` / `PowerCard.vue`：恢复 `.module-card` 卡片结构，`.icon-tile`（104px 圆角方图标，内嵌 1.png / 2.webp，右下角 ▶ 徽标，busy 时 ⏳ + 脉冲动画 + 禁用）点击即执行；标题/描述/tag 回到 module-head 右侧；清体力当前计划并入 desc 文案。
  - `style.css`：删除上一版 .tile-* 整卡背景样式，替换为 .home-banner/.icon-tile/.icon-img/.icon-badge/.icon-pulse。
- 重新 npm run build 成功，8765 实例实时生效（CSS hash index-B1quuzV6.css）。

## 主页卡片精简：只留图标 + 标题

- 用户要求：保留每日实训，清体力当前计划放图标底下即可，其他描述不需要。
- 改动：两卡片删除 desc 段与 tag；图标外包 `.icon-col`（图标 + `.icon-sub` 小字居中）。每日实训 icon-sub = 状态文案（点击图标执行/任务执行中…）；清体力 icon-sub = 当前计划（busy 时为任务执行中…）。HomeView 删除底部 hint 行。style.css 新增 .icon-col/.icon-sub（max-width 104px 允许换行）。
- 重新 build 成功（index-Chr0Dp32.js），8765 实时生效。

## 主页极简化：小图标 + 图标下文字，日志只进日志页

- 用户要求：图标不要占那么大位置，只保留「每日实训」「清体力」文字在图标底下，执行日志信息输出到日志栏目即可。
- 改动：
  - DailyCard / PowerCard：`.icon-tile` 缩到 64px，卡片横向布局（icon-col 左、error-msg 右侧自适应），图标下方 `.icon-label`（14px 加粗「每日实训」/「清体力」）；删除 ProgressBox 进度框与多余状态文字；执行事件仍由 useJobRunner 增量 pushLog 到「日志」页，卡片上只保留 moduleError 错误条。
  - style.css：.home-grid minmax 降为 220px、module-card 改 row 布局、icon-badge 缩到 24px、新增 .icon-label，删除 .icon-sub。
- 重新 build 成功（index-ZRNEizhr.js），8765 实时生效。

## 图标整体化：去边框底板，图标+文字共 104px

- 用户要求：功能图标的内外边距取消，图标和文字当整体放大为 104px。
- 改动（仅 style.css）：.icon-tile 去掉 border/background/box-shadow/padding/margin（72px 裸图，hover 仅微移+提亮）；.icon-col 固定宽 104px、gap/margin 0；.icon-label 高 32px、15px 加粗、居中——72px 图 + 32px 字 = 104px 整体；icon-badge 缩到 22px。
- 重新 build 成功（index-DuYa3rml.css），8765 实时生效。

## 撤销：恢复 64px 带边框图标版本

- 用户要求撤销上一步。style.css 已还原：.icon-tile 恢复 64px + 2px 蓝边 + 白底 + 阴影 + hover 上浮蓝边；.icon-col 恢复 gap 7px、无固定宽；.icon-label 恢复 14px；.icon-badge 恢复 24px。
- 重新 build 成功（index-Drx4R_IR.css），8765 实时生效。

## 卡片最终形态：module-head 内 104px 整体图标 + 右侧报错

- 注意：两轮之间用户手动把 DailyCard/PowerCard 改回了中间版本（含 desc/tag/ProgressBox），本轮基于磁盘现状重构。
- 用户要求：module-head 里只保留运行功能图标 + 底下文字（整体 104px），清体力同理，重要报错信息在图标右半部分展示。
- 改动：
  - DailyCard / PowerCard：module-head = `.icon-col`（裸图 72px + .icon-label 32px，共 104px，无边框无边距）+ `.head-right`（moduleError 错误条，flex:1 垂直居中）；删除 desc/tag/ProgressBox；日志仍由 useJobRunner 增量写入日志页。
  - style.css：.icon-tile 裸图 72px（无 border/bg/shadow），.icon-col 104px/gap0，.icon-label 32px/15px，.icon-badge 22px，.head-right flex:1，.home-grid minmax(300px) 且 module-head min-height 104px。
- 重新 build 成功（index-zjtwT40F.js），8765 实时生效。

## 功能完善：daily_task/clear_power 前置游戏启动检查 + 启动游戏接入

- 需求：点击每日实训/清体力前先用 driver `game/running` 检查游戏是否已启动，未启动则提醒且不执行；「启动游戏」栏接入 driver `game/open` 启动功能。
- **Mod（mods/starrail/mod.py）**：execute_starrail_flow 新增 GAME_REQUIRED_OPERATES={daily_task, clear_power} 前置检查——先 dispatch game/running（检查失败/异常一律视为未启动拦截），未启动返回 status=error「游戏未启动，请先在「启动游戏」中启动…」；新增 game/running、game/open 透传（game/open 归一化 opened 为布尔）。同时补齐测试一直期望但代码缺失的后置回查：daily_task 完成后 dispatch daily_task/observe → result.remaining；clear_power 完成后 dispatch power/query → result.power（失败置 None 不影响主结果）。前置检查通过时不发 event（保持原事件序列）。
- **Web 后端**：bridge.py SYNC_OPERATES + game/running、game/open，新增 GAME_OPEN_TIMEOUT=300s；app.py 新增 GET /api/starrail/game/running（→{running:bool}）、POST /api/starrail/game/open（GameOpenBody{waitForLogin,targetClient}，→{opened:bool}，显式 timeout=300 应对首次全盘搜索），_unwrap_mod_result 解双层包装，operations 文档更新。
- **前端（Vue）**：api.js + gameRunning/gameOpen；useJobRunner 新增 preCheck 钩子（返回非空字符串=拦截，moduleError 显示 + err 日志 + 不发起任务）；DailyCard/PowerCard preCheck 调 gameRunning，未启动提示「请先在启动游戏中启动」；LaunchView 启动按钮接 gameOpen（启动中禁用按钮+文案切换，成功/失败状态条），style.css 新增 .ok-msg。
- controller.py 文档/help 补 game/running、game/open 示例。
- **测试**：test_starrail_mod.py 全部 dispatch/responder mock 补 game/running 分支并更新调用序列断言（原 9 个因缺 remaining/power 后置回查而失败的测试全部修复），新增 6 个测试：game/running、game/open 透传、daily_task/clear_power 未启动拦截、检查失败拦截、mod 级未启动拦截。34 项全过。
- 构建验证：npm run build 成功（index-DfsvFsyI.js）；TestClient 冒烟验证两个新接口（mock bridge）参数透传与响应形状正确。
- 遗留（改动前即存在，与本次无关）：test_starrail_driver/test_smoke 部分用例因 venv 缺 rapidocr_onnxruntime 报错；test_web_backend 2 个 legacy 静态资源用例失败（前端已迁 Vue dist）。

## 清理：移除前端不再使用的功能（死代码）

- 删除 `components/ProgressBox.vue`（卡片精简后无任何引用）。
- `useJobRunner.js`：删除 progressVisible() 与 buildProgressLines()（ProgressBox 专用）及 fmtTime 导入；返回值仅剩 { s, run }。
- `api.js`：删除前端未调用的 dailyObserve / dailyReward / powerQuery（设置页查询按钮早已删除，仅剩死函数）。后端对应接口保留（curl/测试可用）。
- `SettingsView.vue`：删除死代码 queryPower()/showInfo/powerCurrent/powerReserve/queryError 及两个永不显示的 v-if 块；imports 精简。
- `settings.js`：activity 移除永不触发的 query 标记，busyAny/powerBusy 简化为 daily+power / power。
- `HelpView.vue`：操作说明更新为图标点击式 UI（每日实训/清体力图标），新增「② 启动游戏」步骤与未启动拦截说明。
- `style.css`：删除 .progress-log 系列孤儿样式及滚动条选择器中的引用。
- npm run build 通过（JS 85.1→83.0KB，CSS 10.6→10.1KB），grep 确认无残留引用。
