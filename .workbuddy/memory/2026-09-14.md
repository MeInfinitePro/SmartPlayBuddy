# 2026-09-14

## 智玩搭档平台 Mod 挂载（打通并稳定化）

- 平台拓扑（拆前端 JS 确认）：`http://smtplay.cabyss.cn:8080` 是「智玩搭档」SPA（nginx 1.31.5）；`/mods/<mod-id>` 是**前端路由**（ModConsoleView），不是 WS 端点；WS 统一在 `ws://smtplay.cabyss.cn:8080/ws`，与 2508/ws 是**同一后端**（nginx 代理，无 token 均 401）。
- 控制台页行为：连 `ws://<host>/ws`，claim `{type:'web', deviceName: <路由参数 mod-id>}`；`?url=<前端地址>` 会把该地址加载进 sandbox iframe，通过 `__smtplay__` postMessage 信封桥接 WS（iframe 发 `{__smtplay__,hello:true}` 握手 → 双向 `{__smtplay__,payload}` 中继）。
- 挂载协议 = 我们现有的中心 WS 协议（session/claim、command/response、二进制帧）+ 全路由地址 `mod:{userId}:{deviceName}`（裸 deviceName 路由无效，返回 session not found）。
- 端到端验证通过：web 探针 claim → `command/starrail operate=ping` → `To=mod:1971189220:starrail` → mod 回 `{"pong":true,"mod":"starrail"}`。
- **关键发现：服务器在 access_token 过期（899s）后摘除会话但不断 TCP、无任何通知**（连接"假在线"，指令全部 session not found）。mod 连了 2 小时实际早已失效。
- 修复：mods/starrail/mod.py 加 `_closed` Event（on_close 置位+幂等）；__main__.py 改为外层重连循环——每轮 refresh_login→建连→保活观察（15s 轮询，780s 临期且空闲时主动重建，有指令执行中则延迟）。
- 新建项目根 `.env`：`SPB_WS_URL=ws://smtplay.cabyss.cn:8080/ws`（.gitignore 已含 .env），client/mod/webapi 统一走平台入口。
- 待办：① 平台控制台 iframe 需要配套 mod 前端（实现 __smtplay__ 桥的页面，?url= 传入），我们的 web/frontend 需出 iframe 模式或做轻量桥接页；② client 也应有同样的 899s 续期逻辑；③ proxy.yunyuanweigui.cn:5000（他人 mod 前端示例）本机直连不通，仅作参考。

## 两项收尾（10:24）

- **mod 前端桥接页**：`web/bridge/index.html`（纯静态，`__smtplay__` postMessage 桥 + ping/status/power/query/game/running 按钮 + 自定义 operate + 超时/日志）。经 deploy/nginx `/bridge/` 静态服务发布（compose 挂载 `../web/bridge`），localhost 与 172.23.123.73 均 200。用法：`http://smtplay.cabyss.cn:8080/mods/starrail?url=http://172.23.123.73/bridge/index.html`（uid 可 ?uid= 传或页内保存 localStorage）。
- **client 续期**：client.py 复刻 mod 方案——`_closed` Event + `_busy` 计数（驱动 executor 前后增减）+ 外层重连循环（899s TTL，780s 临期且空闲时重建，有任务/活跃流则延迟）。
- 公网托管桥接页（离开本机 nginx 也可用）仍待办；当前对 LAN 内浏览器已完整可用。

## Vue 前端平台模式（替代独立桥接页的正式方案）

- 用户要求挂载的正应该是 web/frontend 开发的前端。已给 Vue 前端加「平台模式」：处于平台 iframe（或 ?platform=1）时，认证跳过（App.vue boot 分支），api.js 的 me/logout/gameRunning/gameOpen/dailyTask/clearPower 切换为 mod 指令（modRequest/modStartJob），pollJob 切换为桥接句柄（modPollJob），UI 与 REST 版完全同构。
- 新文件 web/frontend/src/js/platform.js：__smtplay__ 桥（hello 重发、outbox 缓冲、requestId 匹配、event 投递给最新任务——mod 的 event 不带我们的 requestId 无法按请求路由）。uid 取 ?uid= 或 localStorage（spb_pf_uid）；mod 名取 ?mod= 默认 starrail。
- 已本地 npm build + 重建 webapi 镜像上线（bundle index-M11FsPxG.js），/bridge/ 独立页保留作调试兜底。
- 用法：http://smtplay.cabyss.cn:8080/mods/starrail?url=<URL编码后的 http://172.23.123.73/?platform=1&uid=1971189220>
- 已知局限：mod event 无 requestId，多任务并发时进度会投给最新启动的任务；公网托管待办不变。

## PNA 拦截与前端公网化（10:50）

- 用户在平台 iframe 加载 http://172.23.123.73 被浏览器拦截（Chrome Private Network Access：公网页面连接私网地址）。前端代码无法绕过，页面必须公网托管。
- 用户选择方案：发布 dist 到公网链接。经 workbuddy_sites_deploy（static）发布 web/frontend/dist → https://ec79b4b49f744ae3b577424cb5e33ddb.app.workbuddy.host（bundle index-M11FsPxG.js 已验证 200）。
- 平台挂载地址：http://smtplay.cabyss.cn:8080/mods/starrail?url=<URL编码的 https://.../?platform=1&uid=1971189220>；iframe 为 https 加载无混合内容问题。
- mod 复验在线：e2e ping 返回 {"pong":true,"mod":"starrail"}。
- 备注：LAN 地址 172.23.123.73 入口保留可本机/局域网直用；更彻底方案（前端放平台服务器 8080 同源）仍需服务器权限，待办。

## 修复平台模式 onEvent bug（11:00）

- 症状：平台模式所有 modRequest 一调用即 "onEvent is not defined"（platform.js modRequest 的 entry 引用了未解构的 onEvent 变量）。
- 修复：删除 entry 中的 onEvent 字段（事件路由本就走 latestJobId/jobListeners）。operate 名称与 mod 端（game/running、game/open、daily_task、clear_power、power/query）核对一致。
- 已重新构建（index-DG0BEMQE.js）并更新线上发布链接（同一 shareLink）。

## 前端 game/running 超时根因：双实例抢注（11:06）

- 现象：平台前端 game/running 30s 无响应，但探针直连 mod:1971189220:starrail 秒回。
- 根因：后台挂载实例（--device-name starrail）与用户本地 python -m mods.starrail 实例（--device-name 默认同为 starrail）**同名双 claim**，服务端路由被后 claim 者接管且来回漂移；日志里 "Universe" 是 target_client 非注册名，具误导性。
- 处置：已停掉后台实例 Q8biiI，收敛为用户本地单实例；用户需 Ctrl+C 重启 mod 使新 claim 接管路由。__main__.py 启动日志补打 deviceName（已编译校验）。
- 教训：同一 uid+设备名只允许一个 mod 实例；重启 mod 后必须重启或等待重 claim，否则指令路由仍指向旧会话。

## "还是没连接上"定位（11:43）

- 探针复验：mod:1971189220:starrail 秒回 pong —— mod 后端与平台路由完全正常。
- 真因：平台**控制台页**凌晨打开后一直没刷新，其平台 WS 会话被服务器按 899s token 过期悄悄摘除；iframe 指令经控制台中继进死 WS 消失 → 前端 30s 超时。控制台页是平台代码，无自动续期，只能 F5 刷新。
- 前端改进：platform.js modRequest 超时时区分 handshake 状态，桥未就绪时明确提示"请刷新平台控制台页面"。已重建（index-C_2lsO3f.js）并更新线上。

## 下线前端发布（11:46）

- 应用户要求 unpublish「星穹铁道助手前端」，分享链接 https://ec79b4b49f744ae3b577424cb5e33ddb.app.workbuddy.host 已失效。dist 目录与本地 nginx（172.23.123.73）部署不受影响，需要时可再次发布（同目录、链接会重新生成）。

## 重新发布前端（11:59）

- 应用户要求再次发布，shareLink 不变：https://ec79b4b49f744ae3b577424cb5e33ddb.app.workbuddy.host（bundle index-C_2lsO3f.js 已验证）。平台挂载入口与之前相同。

## 桥接发送门控 bug（真正根因，12:03）

- 拆控制台 modconsole.js 确认：控制台对 iframe 的 hello 只登记 tag（s=n.__smtplay__）、**从不回包**；之后才把后续 payload 转发上 WS。
- 我们前端/兜底页此前把"收到任何回包"当作可发送前提（handshake 门控 + outbox 缓冲，hello 重发 30s 后放弃）——控制台永不回包 → handshake 永不置位 → 所有指令困死在 outbox。这就是"mod 一直收不到前端指令"的真正根因；此前 e2e 验证均走 Python 探针直连 WS，从未经过控制台中继，故未暴露。
- 修复：hello 发出即视为可发送（postMessage 同源有序保证 hello 先于业务消息被登记），hello 改为每 10s 周期重发（兼容控制台页刷新后 tag 清空重登记）；seenInbound 仅用于超时诊断文案。platform.js 与 web/bridge/index.html 同步修复，前端重建（index-BxE3GycR.js）并发布。

## 控制台中继为原始文本，前端需自行解码（12:12）

- 再拆 modconsole.js：`m(e)` 直接挂原始 WebSocket（非解码后的 listener），postMessage 给 iframe 的 payload 是**原始 JSON 字符串**，且协议 data 字段为 base64 —— 前端此前只收对象，全部丢弃（收不到 mod 回包/事件）。
- 修复 platform.js：normalizePayload（JSON.parse 字符串）+ b64ToObject（atob→TextDecoder→JSON）。console.info 标记首次回包到达。
- 新 bundle index-CjuzctoJ.js 已发布。提醒：iframe 可能缓存旧 bundle，测试时内层 URL 加 &v= 参数或控制台 Ctrl+F5。

## 平台中继判死，回归 REST 直连（12:35）

- 手动 postMessage 注入指令（模拟 iframe 完整协议）mod 仍无反应 → 死因在平台控制台→服务器一层（平台代码，不可控），平台挂载暂不可用。我们侧（platform.js 发送/接收解码、mod、路由）均已验证正确。
- 现成可用路：webapi bridge.py（REST → 临时 WS → mod:{uid}:starrail → 收事件/回包）。用户浏览器直开 http://localhost/（localhost 在 authorize 白名单内）走 REST 模式即可控制 mod；172.23.123.73 会被登录白名单卡（服务器侧待改）。
- 结论：平台 iframe 挂载 + 控制台中继方案保留代码但暂停依赖；待服务器可改时优先把前端放 8080 同源 + 改白名单。

## 平台 iframe 挂载重测前排查：.env 优先级 bug（19:55）

- 用户要求重测平台挂载。探针发现 mod 会话 `session not found`（mod 日志却显示连接成功）。
- 排查链：进程/网络正常（TCP ESTABLISHED 经 sandbox-cli 代理）→ 自证探针（claim 自己 ping 自己）证明服务器 claim/路由机制正常 → 本地 ws_echo 回显服务器抓包证明 mod 发出的 claim 报文完全正确 → 全新 mod 以显式 SPB_WS_URL=8080/ws 启动立即注册成功、pong 秒回。
- 根因：config env_file=(".env","deploy/.env") 元组后者优先，deploy/.env（compose 模板，含 2508/ws）覆盖根 .env（8080/ws），mod 静默连到 2508；2508 直连的会话登记行为异常（同载荷探针可注册、mod 进程注册不上，疑似 2508/8080 后端会话视图不一致）。已修 config 仅读根 .env；deploy/.env 保留供 compose。
- 教训：pydantic-settings 多 env_file 元组"后者优先"；模板文件绝不能与真实 .env 同名共存于搜索路径。
- 现状：mod（dh7ifk）以显式 8080/ws 在线、pong 正常，可进行平台 iframe 挂载测试。
